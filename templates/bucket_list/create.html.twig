{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {{ form_start(serieForm) }}

        {{ form_widget(serieForm) }}
        <button>create</button>

        {{ form_end(serieForm) }}
    </div>
{% endblock %}

{% block title %}
    Create a thing to do | {{ parent() }}
{% endblock %}

